---
apiVersion: "logging.openshift.io/v1"
kind: "ClusterLogging"
metadata:
  name: "instance"
  namespace: "openshift-logging"
spec:
  collection:
    logs:
      fluentd: {}
      type: fluentd
---
apiVersion: logging.openshift.io/v1
kind: ClusterLogForwarder
metadata:
  name: instance
  namespace: openshift-logging
spec:
  outputs:
    - cloudwatch:
        groupBy: logType
        groupPrefix: valentine-stage
        region: us-east-1
      name: remote-cloudwatch
      secret:
        name: cloudwatch-forwarder
      type: cloudwatch
  pipelines:
    - inputRefs:
        - application
        - audit
        - infrastructure
      name: fluentd-cloudwatch-logforward
      outputRefs:
        - remote-cloudwatch
